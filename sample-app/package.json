{
  "name": "jakone-devops-contract-mock",
  "version": "2.0.0",
  "private": true,
  "description": "Dependency-free fallback mock aligned to the shared JakOne account-service endpoints for Day 7 DevOps demonstrations",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "build": "node scripts/build.js"
  },
  "engines": { "node": ">=20" }
}
