# Fallback Frontend

Zero-dependency static web client served by Nginx. It reverse-proxies `/api/*` to the `backend` Docker Compose service, avoiding browser CORS problems during the workshop.

Use only as a fallback. Replace this folder with the official frontend when available.
